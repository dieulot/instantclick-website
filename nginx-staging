server {
	server_name instantclick.io;
	root /home/sites/instantclick.io/output;

	expires 30m;

  location ~ ^/(whitelist|start)\.html$ {
    return 302 /download;
  }

  rewrite ^/(changelog|click-test|download|license|github-pages-and-apex-domains)$ /$1.html;
}
