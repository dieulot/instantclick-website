server {
	server_name new.instantclick.io;
	root /home/sites/instantclick.io/output;

	expires 12h;

  location ~ ^/(whitelist|start)\.html$ {
    return 302 /download;
  }

  rewrite ^/(changelog|click-test|download|license)$ /$1.html;
}
